
/**
 * Custom component selector utility
 * This provides similar functionality without Lovable branding
 */
(function() {
  if (process.env.NODE_ENV === 'production') return;
  
  // Basic implementation to support component selection
  console.log('Component selector initialized');
})();
